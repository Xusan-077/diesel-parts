DATABASE_URL="postgresql://user:password@localhost:5432/diesel_parts_erp?schema=public"

PORT=4000

JWT_ACCESS_SECRET="change-me"
JWT_ACCESS_EXPIRES_IN="15m"
JWT_REFRESH_SECRET="change-me"
JWT_REFRESH_EXPIRES_IN="7d"

# Shared secret Next.js and the backend both hold to authenticate
# server-to-server calls that carry a customer's OTP-verified phone number
# (see InternalServiceGuard).
INTERNAL_SERVICE_SECRET="change-me"
